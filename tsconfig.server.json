{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "dist-server",
    "rootDir": "."
  },
  "include": ["server/**/*.ts", "src/**/*.ts", "shared/**/*.ts"],
  "exclude": ["tests", "scripts", "client"]
}
